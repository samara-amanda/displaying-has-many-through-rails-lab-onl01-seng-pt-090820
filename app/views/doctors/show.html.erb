<h1>Doctor Name: <%= @doctor.name %></h1>
<h2>Department: <%= @doctor.department %></h2>
<h3>Appointments:</h3>
<% @doctor.appointments.each do |appt| %>
  <li><%= link_to appt.patient.name, patient_path(appt.patient) %> on <%= appt.appointment_datetime.strftime("%B %d, %Y at %R") %>
<% end %> 